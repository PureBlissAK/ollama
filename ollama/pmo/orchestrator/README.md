# Orchestrator

This package contains scaffolding for cross-repo orchestration: a
`DependencyGraph` helper and a `DeploymentPlan` representation.

The modules are intentionally lightweight and dependency-free so they
can be used in unit tests and as a starting point for integration.
